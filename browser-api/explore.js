const good = () => {
    alert(`Good`);
};
const hood = () => {
    const response = confirm('Are you sure you want to go to the ? ? ');
    console.log(response);
    if (response === true) {
        alert(`You are in the hood`);
    } else { 'G DN' };
};